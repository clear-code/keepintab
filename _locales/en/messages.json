{
  "extensionName": { "message": "KeepInTab + Options" },
  "extensionDescription": { "message": "Removes the target attribute (except for named targets) from links, which forces the browser to open every link in the current tab instead of a new tab or window ." }
}
